# Library (Free Preview Pack)
## Learn the Buyer Engagement Process -- preview only
### Same structure as the full library. Paid depth is absent by design.

**Bottom line:** this free pack ships Title + kicker, Executive summary, Bottom line, and In this entry for entries; worksheets ship title, one-liner, Bottom line, When to use, What it covers, and How to run it. Term cards copy in full (they are already short).

**In here:**
- `entries/` -- PREVIEW-ONLY Full Entry mirrors (gate by absence)
- `terms/` -- term cards (hover-intro source)
- `worksheets/` -- PREVIEW-ONLY worksheet cards
- `paths/` -- thin guided path(s)
- `GLOSSARY.md` -- glossary index
- `preview_html/` -- HTML view exemplars (hover / free-preview cards)
- `index.html` -- branded card index

Pure ASCII. Brand: The Buyer Engagement Sales Engine.

---

## How to use

1. Browse `index.html` or open any `entries/<id>.md` preview.
2. Term cards under `terms/` are the hover-intro source.
3. Add items to `../work/READING_LIST.md` (Add to reading list control in HTML).
4. Open in new tab opens the preview page (`target=_blank` / local open-file equivalent).
5. Upgrade to the full The Buyer Engagement Sales Engine library for Understanding it and Put it into practice.

## Ship rule

Free zip/folder must never contain paid depth files. Re-run `_system/tools/build_free_library.py` after full-library updates.
